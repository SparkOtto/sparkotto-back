model Agencies {
  id_agency       Int        @id @default(autoincrement())
  city            String
  postal_code     Int
  street          String
  additional_info String?
  phone           String
  users           User[]
  vehicles        Vehicles[]
  keys            Keys[]
  head_office     Boolean
  trips_departure  Trips[] @relation("DepartureAgency")
  trips_arrival    Trips[] @relation("ArrivalAgency")
}

model Domaines {
  id_domaine   Int    @id @default(autoincrement())
  domaine_name String @unique
}
